apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mon-app.fullname" . | trunc 63 }}
  labels:
    app: {{ include "mon-app.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "mon-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "mon-app.name" . }}
    spec:
      containers:
        - name: {{ include "mon-app.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 80
