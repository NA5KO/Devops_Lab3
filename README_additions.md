This repo now contains:

- `Dockerfile` and `index.html` (simple static app)
- `k8s/` with `deployment.yaml` and `service.yaml` for non-Helm deployment
- `mon-app/` Helm chart (Chart.yaml, values.yaml, templates/)
- `Jenkinsfile-nohelm` and `Jenkinsfile-helm` pipeline examples

See the main README.md for instructions on running Jenkins and configuring credentials.
